server:
  host: fl-server-service
  port: 8080

dataset:
  type: "5gnidd"
  test_size: 0.2
  seed: 42
  data_dir: "/app/data"

num_clients: 3  # Changed from 1
num_classes: 9  # 5G-NIDD has 9 attack types
batch_size: 256

attack:
  enabled: false

config_fit:
  lr: 0.01
  momentum: 0.9
  local_epochs: 1
