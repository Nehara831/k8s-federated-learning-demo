apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dataset-cache-pvc
  namespace: federated-learning
spec:
  accessModes:
    - ReadWriteMany  
  resources:
    requests:
      storage: 2Gi
